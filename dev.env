# Development environment for Forkline
# Source this file to set up your environment:
#   source dev.env
#
# Or run commands with it:
#   source dev.env && python examples/minimal.py

export PYTHONPATH="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

echo "✓ PYTHONPATH set to: $PYTHONPATH"
echo "✓ Ready to run Forkline examples and tests"
